# Dynamic Logger
Logger object for Python.

## Usage
Prints messages (`str`) if their debug level is equal or less than the object's debug level.
Can also measure execution time. Multiple timers can be started and kept in a nested fashion (FIFO)
